{
  "name": "@pantsbuild/example-serverless",
  "version": "1.0.0",
  "description": "Example package for serverless",
  "scripts": {
    "test": "jest",
    "build": "esbuild ./aws/javascript/* --entry-names=[dir]/[name]/index --bundle --minify --sourcemap --platform=node --target=node16.14 --outdir=. && cd handler && zip handler.zip *"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pantsbuild/example-serverless.git"
  },
  "dependencies": {
    "jest": "^29.7.0",
    "esbuild": "^0.21.4"
  },
  "private": true
}
